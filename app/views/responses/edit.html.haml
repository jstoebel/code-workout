%h1 Edit Response

=render(:partial => 'application/record_error_messages', :locals => {:record => @response})

= form_for(@response, url: response_path) do |f|
  = f.label(:text, "Edit your response:")
  %br/
  = f.text_area(:text, value: @response.text)
  = submit_tag("Submit")

